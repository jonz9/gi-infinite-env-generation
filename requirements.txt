# The harness core (validate / prove / edit / PNG frames) is pure Python standard
# library — no runtime dependencies, no model SDK, no API key. The agent that runs
# the repo is the planner.

# Test suite:
pytest

# Physics engine + game window (the primary way to play generated environments;
# the harness degrades gracefully to stdlib PNG frames + ASCII without them).
# NOTE: install the `Box2D` distribution, not `box2d-py` (which fails to build
# on Apple Silicon).
pygame
Box2D
